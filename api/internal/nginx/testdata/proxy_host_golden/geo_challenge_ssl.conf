# nginx-guard generated config
# Proxy Host ID: 00000000-0000-0000-0000-000000000a0c
# Domain(s): challenge-ssl.example.com
# WAF: disabled
# HTTP/3: disabled
# Access List: none
# Geo Restriction: blacklist (1 countries)
# Rate Limit: none
# Upstream: none
# Generated at: auto-generated










# Direct-forward upstream (keepalive connection pool to the backend)
# proxy_params.conf already sets the prerequisites: proxy_http_version 1.1 +
# empty Connection header. WebSocket hosts override Connection per-location
# with $connection_upgrade and simply don't benefit from the pool.
upstream npg_direct_00000000_0000_0000_0000_000000000a0c {
    server 127.0.0.1:8080;
    keepalive 32;
    keepalive_requests 1000;
    keepalive_timeout 60s;
}



server {
    listen 80;
    listen [::]:80;
    server_name challenge-ssl.example.com;

    # Initialize tracking variables
    set $block_reason_var "-";
    set $bot_category_var "-";
    set $geo_blocked 0;
    set $is_search_bot 0;

    # NPM-compatible variables for custom config
    set $forward_scheme http;
    set $server "127.0.0.1";
    set $port 8080;

    # Shared host stanza: resolver, ACME/challenge skip vars, custom error pages
    include /etc/nginx/conf.d/includes/host_common.conf;



    # ACME HTTP-01 Challenge support (bypass all security checks)
    location /.well-known/acme-challenge/ {
        # Allow all access for certificate validation
        allow all;
        root /etc/nginx/acme-challenge;
        try_files $uri =404;
    }




    # WAF disabled for this host (override global modsecurity on)
    modsecurity off;



    # Geo Restriction (blacklist) - Challenge Mode (processed AFTER WAF)
    # Challenge mode: show CAPTCHA instead of blocking
    set $geo_blocked 0;

    if ($geoip2_country_code ~ "^(CN)$") {
        set $geo_blocked 1;
        set $block_reason_var "geo_block";
    }


























    # Challenge validation endpoint (internal)
    location = /_challenge/validate {
        internal;
        # Local decision for cookieless geo-blocked visitors: 401 here (the
        # caller's error_page 401 turns it into the challenge redirect) with
        # no API round-trip. Everyone else falls through to token validation.
        set $challenge_gate 0;
        if ($geo_blocked = 1) {
            set $challenge_gate 1;
        }

        set $challenge_gate_final $challenge_gate;
        if ($cookie_ng_challenge = "") {
            set $challenge_gate_final "${challenge_gate}1";
        }
        if ($challenge_gate_final = 11) {
            return 401;
        }
        proxy_pass http://127.0.0.1:9080/api/v1/challenge/validate;
        proxy_pass_request_body off;
        proxy_set_header Content-Length "";
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Proxy-Host-ID "00000000-0000-0000-0000-000000000a0c";
        proxy_set_header X-Challenge-Token $cookie_ng_challenge;
        proxy_set_header X-Geo-Blocked $geo_blocked;
        # Fast timeout - fail fast if API is down
        proxy_connect_timeout 2s;
        proxy_read_timeout 5s;
    }

    # API error fallback - allow traffic through when API is down
    location @api_fallback {
        # API is down - allow request to proceed (graceful degradation)
        # Log this event for monitoring
        access_log /etc/nginx/logs/access_raw.log main buffer=64k flush=5s;
        proxy_pass http://npg_direct_00000000_0000_0000_0000_000000000a0c;
        # Direct-forward upstream is an upstream group name, so `proxy_redirect
        # default` can't derive the backend URL — rewrite backend Location/Refresh explicitly.
        proxy_redirect http://127.0.0.1:8080/ /;
        include /etc/nginx/includes/proxy_params.conf;
    }

    # Challenge API - Bypass GeoIP and proxy to API service (HTTP server block)
    location /api/v1/challenge/ {
        modsecurity off;

        proxy_pass http://127.0.0.1:9080/api/v1/challenge/;
        proxy_pass_request_body on;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
    }





    # Redirect HTTP to HTTPS
    
    # Server-level redirect — survives any user-defined location block.
    # Bypass: ACME HTTP-01 (cert renewal) + NPG challenge page (geo restriction).
    if ($request_uri !~ "^/\.well-known/acme-challenge/|^/api/v1/challenge/") {
        return 301 https://$host$request_uri;
    }
    







}


server {
    listen 443 ssl;
    listen [::]:443 ssl;


    server_name challenge-ssl.example.com;

    # Initialize tracking variables
    set $block_reason_var "-";
    set $bot_category_var "-";
    set $geo_blocked 0;
    set $is_search_bot 0;

    # NPM-compatible variables for custom config
    set $forward_scheme http;
    set $server "127.0.0.1";
    set $port 8080;

    # Shared host stanza: resolver, ACME/challenge skip vars, custom error pages
    include /etc/nginx/conf.d/includes/host_common.conf;



    # SSL configuration
    ssl_certificate /etc/nginx/certs/00000000-0000-0000-0000-0000000000ce/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/00000000-0000-0000-0000-0000000000ce/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305;
    ssl_ecdh_curve X25519MLKEM768:X25519:secp256r1:secp384r1;





    # WAF disabled for this host (override global modsecurity on)
    modsecurity off;



    # Geo Restriction (blacklist) - Challenge Mode (processed AFTER WAF)
    # Challenge mode: show CAPTCHA instead of blocking
    set $geo_blocked 0;

    if ($geoip2_country_code ~ "^(CN)$") {
        set $geo_blocked 1;
        set $block_reason_var "geo_block";
    }


























    # Challenge validation endpoint (internal)
    location = /_challenge/validate {
        internal;
        # Local decision for cookieless geo-blocked visitors: 401 here (the
        # caller's error_page 401 turns it into the challenge redirect) with
        # no API round-trip. Everyone else falls through to token validation.
        set $challenge_gate 0;
        if ($geo_blocked = 1) {
            set $challenge_gate 1;
        }

        set $challenge_gate_final $challenge_gate;
        if ($cookie_ng_challenge = "") {
            set $challenge_gate_final "${challenge_gate}1";
        }
        if ($challenge_gate_final = 11) {
            return 401;
        }
        proxy_pass http://127.0.0.1:9080/api/v1/challenge/validate;
        proxy_pass_request_body off;
        proxy_set_header Content-Length "";
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Proxy-Host-ID "00000000-0000-0000-0000-000000000a0c";
        proxy_set_header X-Challenge-Token $cookie_ng_challenge;
        proxy_set_header X-Geo-Blocked $geo_blocked;
        # Fast timeout - fail fast if API is down
        proxy_connect_timeout 2s;
        proxy_read_timeout 5s;
    }

    # API error fallback - allow traffic through when API is down
    location @api_fallback {
        # API is down - allow request to proceed (graceful degradation)
        access_log /etc/nginx/logs/access_raw.log main buffer=64k flush=5s;
        proxy_pass http://npg_direct_00000000_0000_0000_0000_000000000a0c;
        # Direct-forward upstream is an upstream group name, so `proxy_redirect
        # default` can't derive the backend URL — rewrite backend Location/Refresh explicitly.
        proxy_redirect http://127.0.0.1:8080/ /;
        include /etc/nginx/includes/proxy_params.conf;
    }



    # Challenge API - Bypass all security and proxy to API service
    # Generated for both GeoRestriction ChallengeMode and CloudProvider ChallengeMode
    location /api/v1/challenge/ {
        # Disable WAF for challenge parameters (return URL triggers SQLi/RFI rules)
        modsecurity off;

        proxy_pass http://127.0.0.1:9080/api/v1/challenge/;
        proxy_pass_request_body on;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        # Use $remote_addr directly to prevent X-Forwarded-For header spoofing
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
    }







    location / {

        # Challenge decision happens inside the /_challenge/validate gate, which
        # runs as an auth_request subrequest (ACCESS phase) - so ModSecurity
        # phase 2 inspects the request BEFORE any challenge redirect. A
        # rewrite-phase `return 302` here would silently skip phase 2.
        # Cookieless geo-blocked visitors get a local 401 from the gate (no API
        # round-trip); API returns 200 for search bots, so they aren't redirected.
        auth_request /_challenge/validate;
        error_page 401 = @challenge_redirect;
        error_page 500 502 503 504 = @api_fallback;


        proxy_pass http://npg_direct_00000000_0000_0000_0000_000000000a0c;
        # Direct-forward upstream is an upstream group name, so `proxy_redirect
        # default` can't derive the backend URL — rewrite backend Location/Refresh explicitly.
        proxy_redirect http://127.0.0.1:8080/ /;
        include /etc/nginx/includes/proxy_params.conf;
        
        # Proxy settings (Host-level overrides Global)
        
        
        
        
        
        
        
        
        # Proxy buffer settings (from Global Settings)
        
        
        
        
        
        # Bandwidth limiting (from Global Settings)
        
        
        
        
        
        # Clear Alt-Svc cache when HTTP/3 is disabled (helps Safari recover faster)
        add_header Alt-Svc 'clear' always;
        
        
        

    }


    location @challenge_redirect {
        return 302 /api/v1/challenge/page?host=00000000-0000-0000-0000-000000000a0c&reason=geo_restriction&return=https://$host$request_uri;
    }









}


