# Nginx Proxy Guard - Environment Configuration
# Copy this file to .env and modify as needed:
#   cp env.example .env

# ===========================================
# Required Settings
# ===========================================

# Database password - CHANGE THIS!
# Generate: openssl rand -base64 24
DB_PASSWORD=change-me-in-production

# JWT secret for API authentication - CHANGE THIS!
# Generate: openssl rand -hex 32
JWT_SECRET=change-me-in-production

# ===========================================
# Optional Settings
# ===========================================

# Timezone (default: UTC)
TZ=Asia/Seoul
DB_USER=postgres
DB_NAME=nginx_proxy_guard

# ===========================================
# Docker Compatibility (Synology DSM, etc.)
# ===========================================

# Docker API version - leave empty for auto-detection (recommended)
# Only set this if auto-detection fails
# Synology DSM typically uses: 1.41 or 1.43
# DOCKER_API_VERSION=1.41

# ===========================================
# Custom Ports (for host network mode)
# ===========================================

# Admin panel port (default: 81)
# UI_PORT=81

# Nginx listen ports - use with network_mode: host
# When other services use 80/443 (e.g., Synology Web Station)
# NGINX_HTTP_PORT=8080
# NGINX_HTTPS_PORT=8443

# API host port - MUST NOT conflict with NGINX_HTTP_PORT
# Required when nginx uses host network mode
# API_HOST_PORT=9080

